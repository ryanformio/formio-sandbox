var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import { Components } from 'formiojs';
var baseEditForm = Components.components.component.editForm;
import DataTableEditData from './editForm/DataTable.edit.data';
import DataTableEditDisplay from './editForm/DataTable.edit.display';
import DataTableEditValidation from './editForm/DataTable.edit.validation';
import DataTableEditFetch from './editForm/DataTable.edit.fetch';
export default function () {
    var extend = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        extend[_i] = arguments[_i];
    }
    return baseEditForm.apply(void 0, __spreadArrays([[
            {
                key: 'display',
                components: DataTableEditDisplay
            },
            {
                label: 'Fetch',
                key: 'fetch',
                weight: 25,
                components: DataTableEditFetch,
            },
            {
                key: 'data',
                components: DataTableEditData
            },
            {
                key: 'validation',
                components: DataTableEditValidation
            },
            {
                key: 'addons',
                ignore: true
            },
        ]], extend));
}
