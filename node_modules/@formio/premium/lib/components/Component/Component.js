import { Components } from 'formiojs';
import editForm from './Component.form';
import PremiumUtils from '../../utils';
var BaseComponent = Components.components.component;
BaseComponent.prototype.afterComponentAssign = function () {
    if (this.builderMode) {
        PremiumUtils.transformSimpleConditions(this.component);
    }
};
Object.defineProperty(BaseComponent, 'simpleConditionSettings', {
    enumerable: true,
    configurable: true,
    get: function () {
        return {
            operators: ['isEqual', 'isNotEqual', 'isEmpty', 'isNotEmpty'],
            valueComponent: function () {
                return { type: 'textfield' };
            }
        };
    }
});
Components.baseEditForm = editForm;
BaseComponent.editForm = editForm;
export default BaseComponent;
