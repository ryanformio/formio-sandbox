import { BaseEntity } from '../BaseEntity';
export declare class ValueSource extends BaseEntity {
    static get weight(): number;
    static getInputEditForm(options: any): any;
    getValue(input: any): void;
}
