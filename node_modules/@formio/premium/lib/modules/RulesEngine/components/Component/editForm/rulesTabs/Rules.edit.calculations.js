var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import EditFormUtils from '../utils';
import BaseData from 'formiojs/components/_classes/component/editForm/Component.edit.data';
export default [
    EditFormUtils.getPanel('Advanced Default Value', 'customDefaultValue', 0, null, __spreadArrays(EditFormUtils.getModesComponents('Create default value using:', 'customDefaultValue', 0, {
        ui: EditFormUtils.advancedDefaultValueEditFormComponents.ui(),
        js: EditFormUtils.advancedDefaultValueEditFormComponents.js(),
        json: EditFormUtils.advancedDefaultValueEditFormComponents.json(),
    }))),
    EditFormUtils.getPanel('Calculated Component Value', 'calculateValue', 10, null, __spreadArrays(EditFormUtils.getModesComponents('Create value using:', 'calculateValue', 0, {
        ui: EditFormUtils.calculatedValueEditFormComponents.ui(),
        js: EditFormUtils.calculatedValueEditFormComponents.js(),
        json: EditFormUtils.calculatedValueEditFormComponents.json(),
    }), BaseData.filter(function (setting) { return ['calculateServer', 'allowCalculateOverride'].includes(setting.key); }))),
];
