var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import EditFormUtils from '../utils';
import BaseValidation from 'formiojs/components/_classes/component/editForm/Component.edit.validation';
export default [
    EditFormUtils.getPanel('Simple Validation', 'simpleValidation', 0, null, __spreadArrays(BaseValidation.filter(function (setting) { return !['custom-validation-js', 'json-validation-json'].includes(setting.key); }))),
    EditFormUtils.getPanel('Advanced Validation', 'advancedValidation', 40, null, [EditFormUtils.getAdvancedValidationsEditForm()]),
];
