var __spreadArrays = (this && this.__spreadArrays) || function () {
    for (var s = 0, i = 0, il = arguments.length; i < il; i++) s += arguments[i].length;
    for (var r = Array(s), k = 0, i = 0; i < il; i++)
        for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
            r[k] = a[j];
    return r;
};
import { Formio } from 'formiojs';
import EditFormUtils from './utils';
var validations = Formio.rulesEntities
    ? __spreadArrays([
        {
            key: 'quickRulesHeading',
            type: 'htmlelement',
            input: false,
            tag: 'h4',
            content: 'Quick Rules',
        }
    ], [
        Formio.rulesEntities.QuickRules.getQuickRule('minLength'),
        Formio.rulesEntities.QuickRules.getQuickRule('required'),
        Formio.rulesEntities.QuickRules.getQuickRule('maxLength'),
        Formio.rulesEntities.QuickRules.getQuickRule('minWords'),
        Formio.rulesEntities.QuickRules.getQuickRule('maxWords'),
        Formio.rulesEntities.QuickRules.getQuickRule('pattern'),
    ]
        .map(function (item) {
        return EditFormUtils.addQuickRule(item);
    })) : [];
export default validations;
